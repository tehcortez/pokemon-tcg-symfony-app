{% extends 'base.html.twig' %}

{% block title %}Error {{ status_code }} - {{ status_text }}{% endblock %}

{% block body %}
    <h1>An error occurred: {{ status_code }} - {{ status_text }}</h1>
    <p>{{ exception.message }}</p>
    <p><a href="{{ path('pokemon_card_list') }}">Return to Home</a></p>
{% endblock %}
